services:
  - type: web
    name: ghost-ledger
    runtime: node
    plan: free
    buildCommand: "npm install --prefix core && npm run build --prefix core && npm install --prefix server && npm run build --prefix server"
    startCommand: "node server/dist/server.js"
    envVars:
      - key: PORT
        value: "8600"
